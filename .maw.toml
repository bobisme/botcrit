[merge]
# Auto-resolve conflicts in these paths by taking main's version
# .crit/ contains append-only event logs that should not be overwritten by stale workspace data
# .beads/ contains issue tracking that changes frequently on main
auto_resolve_from_main = [
  ".crit/**",
  ".beads/**",
]
